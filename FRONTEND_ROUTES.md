# FargeSpace Think Tank 前台路由冻结（T701）

版本：V1  
日期：2026-04-09  
状态：Frozen

## 1. 目的

冻结 Phase 7 的路由结构，后续页面实现不再改路径形态。

## 2. 路由清单

| 页面 | 路由 |
|---|---|
| 首页 | `/` |
| 主题页 | `/topics/[slug]` |
| 合集页 | `/collections/[slug]` |
| 内容包详情页 | `/packages/[slug]` |
| 搜索页 | `/search` |
| 登录页 | `/login` |

## 3. 路径参数规则

- `slug` 统一使用内容层已有 slug 字段
- 不新增额外路径层级
- 不使用 query 作为主路径语义

## 4. 当前实现状态

以上 6 条路由已经生成可访问页面占位，并通过构建检查。

## 5. 下一步

进入 T702：

- 实现页面共享框架（页头、导航、登录状态区、页宽规则、页脚）
